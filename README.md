# ИДЗ №2

## Задание

Вариант **9**
- Задача **читателей-писателей**
- Листинг **алг. 6.21** из Бен-Ари
- Писатель отпускал всех приостановленных читателей, если они есть.
- **Техника решения:** Метод передачи эстафеты - сумма значений счетчиков семафоров не более 1
(пример техники приведен в Бен-Ари на стр.143-144)
- **Свойства (4)** Темпоральные свойства выражаются с помощью атомарных предикатов
- **Свойства (5)** Темпоральные свойства выражаются с помощью атомарных высказываний. На базе **ltl**.
- **Сложность:** простая

## Установка зависимостей

P.S. `tcl` и `tk` должны быть 8 версии!

Для `Debian/Ubuntu`

```bash
sudo apt install yacc tcl tk gcc make
```

Для `Fedora`

```bash
sudo dnf install yacc tcl8 tk8 gcc make
```

## Установка Spin

Для `Debian`

```bash
sudo apt install spin
```

Для `Ubuntu/Fedora`

```bash
git clone https://github.com/nimble-code/Spin
cd Spin
make
sudo make install
```