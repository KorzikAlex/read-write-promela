**Перевод:**

Алгоритм 6.21 - это решение проблемы читателей и писателей с использованием семафоров. SignalProcess - это подпрограмма, которая выполняется после каждой начальной и конечной операций.

- (a) Докажите, что entry, readerSem и writerSem образуют разделенную двоичную семафору, то есть докажите, что

$0 <= entry + readerSem + writerSem <= 1$

- Докажите правильность алгоритма, показав инвариантность:

$(writers > 0) \to (writers = 1)  \land (readers = 0)$

- (b) Подпрограмма SignalProcess является единой для всех операций. Упростите алгоритм, написав отдельный код для каждой из операций.
- (c) Покажите, что читателям предоставляется приоритет перед писателями.
- (d) Измените алгоритм, чтобы предоставить писателям приоритет перед читателями.
- (e) Измените алгоритм, чтобы предоставить читателям и писателям альтернативный приоритет.

**Оригинал:**

Algorithm 6.21 is a solution of the problem of the readers and writers using semaphores. SignalProcess is a subprogram that is executed following each of the start and end operations.
- (a) Prove that entry, readerSem and writerSem form a split binary semaphore, that is, prove that

$0 <= entry + readerSem + writerSem <= 1$

- Prove the correctness of the algorithm by showing the invariance of:

$(writers > 0) \to (writers = 1)  \land (readers = 0)$

- (b) The subprogram SignalProcess is uniform for all the operations. Simplify the algorithm by writing separate code for each of the operations.
- (c) Show that readers are given precedence over writers.
- (d) Modify the algorithm to give writers precedence over readers.
- (e) Modify the algorithm to give alternate precedence to readers and writers.